

<h1>Photo Book</h1>

<%= bootstrap_form_for @photo do |f| %>

	<%= f.text_field :title %>

	
	<%= f.file_field :picture %>
	<%= f.submit %>
<% end %>

<div>
	<% @photos.each do |photo| %>
		<%= cl_image_tag(photo.picture) %>
		<%= photo.title %>
	<% end %>
</div>